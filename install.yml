---
- name: "Create inventory with target host(s)"
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - include_role:
        name: inventory_helper
        tasks_from: create_inventory.yml

- name: "Dummy - install"
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - ansible.builtin.debug:
        msg: "dummy install.yml"
